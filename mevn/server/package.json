{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:server": "nodemon server.js --ignore '/client/'",
    "start:client": "nodemon client.js --ignore '/server/'"
  },
  "repository": {
    "type": "git",
    "url": "#"
  },
  "author": "damdam",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "express": "^4.18.1",
    "mongoose": "^6.4.6"
  }
}
